unit UGostergeler;

interface

uses
  SysUtils, Windows, Messages, Classes, Graphics, Controls,
  StdCtrls, Forms, DBCtrls, DB, DBGrids, DBTables, Uni, DBAccess, MemDS, Uni, DBAccess, MemDS, Grids, Mask, ExtCtrls,
  Buttons;

type
  TFGostergeler = class(TForm)
    DBGrid1: TDBGrid;
    Vazgec: TBitBtn;
    procedure BitBtn1Click(Sender: TObject);
    procedure BitBtn4Click(Sender: TObject);
    procedure VazgecClick(Sender: TObject);
  private
    { private declarations }
  public
    { public declarations }
  end;

var
  FGostergeler: TFGostergeler;

implementation

uses UMEMUR;

{$R *.DFM}


procedure TFGostergeler.BitBtn1Click(Sender: TObject);
begin
Close;
end;


procedure TFGostergeler.BitBtn4Click(Sender: TObject);
begin
Close;
end;


procedure TFGostergeler.VazgecClick(Sender: TObject);
begin
Close;
end;

end.
